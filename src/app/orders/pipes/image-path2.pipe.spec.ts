import { ImagePath2Pipe } from './image-path2.pipe';

describe('ImagePath2Pipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePath2Pipe();
    expect(pipe).toBeTruthy();
  });
});
